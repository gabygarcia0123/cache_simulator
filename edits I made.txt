Okay I made some changes so let me know what you think

1. You had page_size = params.b * 1  # assumption: page size = block size
this could creat inconsistent results if are page size is not consistent so I changed to:
page_size = 4096

2. For this code it seems you are using block size instead of page size. Im not sure that is correct
page_size = params.b
physical_pages = physical_memory_bytes // page_size

I changed it to:
page_size = 4096
physical_pages = physical_memory_bytes // page_size 

3. I tweaked the fault logic because it is only counting a page fault right now when memory is full. 
if free_pages:
    ...
else:
    stats["page_faults"] += 1
	
I changed it to 
if virtual_page in page_tables[pid]:
    stats["page_hits"] += 1
else:
    stats["page_faults"] += 1   # <-- MOVE HERE

    if free_pages:
        phys_page = free_pages.pop(0)
        page_tables[pid][virtual_page] = phys_page
        stats["pages_from_free"] += 1
    else:
        evict_vpage = next(iter(page_tables[pid]))
        evict_ppage = page_tables[pid].pop(evict_vpage)
        page_tables[pid][virtual_page] = evict_ppage
		
4. I changed some hardcoded bits issues
this is what i put:
wasted_bytes = (total_entries - used) * entry_size_bits // 8

